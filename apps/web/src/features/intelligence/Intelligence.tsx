import type { Signal } from '../../types/api'; import { Panel, State } from '../../components/UI'
export function Intelligence({ signals, onAccount }: { signals: Signal[]; onAccount: (id: string) => void }) { return <div className="surface"><div className="page-title"><span className="eyebrow">Source-normalized signals</span><h1>Intelligence</h1></div><Panel title="Ranked commercial signals"><div className="signal-grid">{signals.map(signal => <article className="card" key={signal.id}><div className="row"><State value={signal.kind} /><State value={signal.evidence_state} /></div><h3>{signal.title}</h3><p>{signal.relevance_explanation}</p><p className="muted">Program: {signal.program_name ?? 'not resolved'}</p><a href={signal.source_url} target="_blank" rel="noreferrer">Source lineage ↗</a>{signal.account_id && <button onClick={() => onAccount(signal.account_id!)}>Open related account</button>}</article>)}</div></Panel></div> }
