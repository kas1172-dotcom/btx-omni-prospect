import type { MapRecord } from '../../types/api'; import { Panel, State } from '../../components/UI'
export function Map({ records, layers, onAccount }: { records: MapRecord[]; layers: string[]; onAccount: (id: string) => void }) { return <div className="surface"><div className="page-title"><span className="eyebrow">National commercial graph</span><h1>Map</h1><p>Top-100 market layers with proximity for seller planning only.</p></div><Panel title="Layers"><div className="layer-row">{layers.map(layer => <span key={layer}>{layer}</span>)}</div></Panel><Panel title="Account geography"><div className="map-grid">{records.map(record => <button className="map-point" key={record.account_id} onClick={() => onAccount(record.account_id)}><span className="rank">#{record.external_rank}</span><strong>{record.industry}</strong><State value={record.relationship} /><small>{record.latitude}, {record.longitude}<br />Nearest: {record.nearest_btx_facility.name}<br />Planning input: {record.proximity_input}{record.deep_account ? ' · deep context' : ''}</small></button>)}</div></Panel></div> }
