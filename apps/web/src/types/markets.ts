export type MarketTransformation = 'LEVEL' | 'MOM_PERCENT' | 'YOY_PERCENT'
export type MarketPoint = { period: string; value: string | null; status: string }
export type MarketMetadata = { id: string; native_code: string; title: string; naics: string; markets: string[]; limitation: string; metric: string; unit: string; index_base: string; taxonomy: string; frequency: string; seasonal_adjustment: string; geography: string; publisher: string; source_url: string; metadata_url: string; license: string; registry_version: string }
export type MarketSeries = { metadata: MarketMetadata; status: string; latest: MarketPoint | null; points: MarketPoint[]; vintage_id: string | null; last_verified_at: string | null }
export type MarketHealth = { last_run: { id: string; status: string; reason?: string; retrieved_at?: string; completed_at?: string } | null; schedule_status: string; vintage_list_limit: number }
export type MarketOverview = { registry_version: string; transformation: MarketTransformation; moving_average_months: number | null; series: MarketSeries[]; coverage: Array<{ market: string; series_ids: string[]; status: string; missing_reason: string | null; regional_status: string; aggregation: string }>; health: MarketHealth; score_effect: string; authority: string }
export type MarketDetail = { metadata: MarketMetadata; points: MarketPoint[]; vintage_id: string; is_current: boolean; retrieved_at: string; last_verified_at: string | null; source_sha256: string; release_date: string | null; source_url: string; source_title: string; retrieval_kind?: string; observations: Array<MarketPoint & { source_line: number }>; transformation: MarketTransformation; moving_average_months: number | null; display_unit: string; vintages: Array<{ vintage_id: string; retrieved_at: string }>; source_health: MarketHealth; interpretation: string }
