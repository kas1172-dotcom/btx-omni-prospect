# Opportunities UI decisions — 2026-09-20

## Scope and baseline

- Created `opportunities-ui-rebuild` in the isolated worktree `C:/Users/Aruna/btx-opportunities-ui-rebuild` from `a23da6e`. The original worktree's Communications, App and presentation-test edits are excluded. Alternative rejected: carrying unrelated uncommitted work into the task branch.
- Baseline checks started before implementation. This commit has 84 frontend tests (the earlier audit had 82). Backend uses the already-installed Python environment with this worktree's `src` on PYTHONPATH; no shared or production database is configured. Tests own temporary databases. No migrations will be created or applied to application databases.
- Figma MCP is available. Read all seven requested frames: 392:93, 394:93, 394:385, 397:93, 397:403, 398:93 and 398:193, using the Figma design-to-code skill. Adapt these to the existing shell; do not rebuild unrelated navigation or authentication.

## Data and semantics

- Use `opportunity_priority` as the single displayed Priority decision. It already uses the six fixed weights 30/25/15/10/10/10 (`modules/scoring/families.py`, `account_attractiveness.py`). Do not recompute its point score, range, contributions, gates or coverage. Alternative rejected: retaining a second Attractiveness summary of the same rubric.
- The API's existing `gates.qualified_and_durable` and `gates.durable_best_bet` drive saved views and whole-lane metrics. Extend frontend types for these existing fields; do not derive eligibility in the browser.
- Market and BU are absent from the workspace response. Add only these optional projection fields, with tests. Use existing market assignments/primary flag where present, otherwise alphabetically choose from canonical account industries. Use an explicitly owned opportunity/account BU when available; missing ownership stays null. Do not choose an arbitrary BU from a multi-BU account.
- No screen currently says "Active Opportunities": the federal tab is already "Opportunity pipeline" within Federal Procurement. Skip the conditional rename; do not change federal IDs or routes.
- A development-only, explicitly opted-in fixture will support the requested rich states. Production must eliminate both the fixture import and data. Never silently replace empty/error API responses. Fixture records must not be sent as real opportunity context to Omni or queried as persisted evidence.
- Reuse the shared Drawer focus/escape/backdrop behavior, CommercialEvidence and existing score presentation for auxiliary decisions. Adapt drawer layout locally without changing shared signatures.
- Whole-lane value metrics and group totals sum sized records per currency, never combine currencies or treat null values as zero-sized pursuits. Label missing sizes. Market facet options come from returned rows across both lanes; counts reflect the current lane and other active filters, excluding the market selection itself.
- Preserve legacy `record` selection links; store new view/filter/group state under existing `f.*` query conventions and `sort`. Persist collapsed group names as well so shared views restore fully.

## Judgments and blocker log

- Baseline measured at this HEAD is 84 frontend passed; backend 797 passed, 16 failed, 10 errors, 2 skipped. The supplied older audit is not the comparison baseline. Sample-hash failures did not recur in this checkout; no sample/hash changes made.
- Market: explicit ledger primary/is_primary flag wins, otherwise canonical account industries sorted case-insensitively; no guessed NAICS mapping. BU: matching canonical CRM deal's business_unit, scoped by account and opportunity ID. An account's general list of business units is not an owner, so that fallback was rejected.
- Stable sort: complete scores always precede incomplete, even in ascending priority mode; missing priority/value and Unassigned market stay last. Every tie ends with opportunity ID. Different currencies retain separate subtotals and sort first by currency rather than inventing exchange rates.
- Selection uses existing record query parameter instead of a new duplicate f.opportunity parameter, preserving account links. View state extends f.*; groups also persist collapsed names. Previous/next traverses group order and the sort order within groups, including collapsed groups; collapsing is presentation, not filtering.
- Only the selected lane's whole-data metrics are shown; text/facet/saved filters never change those metrics. Zero-sized value is real; null is Not sized.
- Missing factor contributions render Unknown, never derived from weight/points. The fixture contains authored score/factor examples; application code never computes a composite. Pwin/delivery details are retained under Evidence using the existing ScoreSummary.
- Mobile omits the metric cards as in the supplied mobile frame. It retains a filter search field and uses bottom sheets for the same desktop controls. Existing shell/sidebar/mobile navigation are retained, not redesigned.
- Existing app declares Inter/Archivo but does not bundle them. Added two small self-hosted Latin WOFF2 assets under screen-specific family aliases, with original SIL OFL licenses; no dependency added and no runtime font-provider request. Non-Latin characters use system fallback.
- The requested fixture is explicitly enabled only with f.opportunity_fixture=demo in a development build. It never substitutes for an API error or empty response. The UI labels the fixture, does not fetch fake evidence records, and disables canonical account/Omni actions for illustrative IDs. Production tree-shaking is verified separately.
- Existing durable Playwright startup fails before tests: no qualified commercial import. Rejected changing the pinned hash or importing data. Added a dedicated Playwright config using the real backend with durable modes off and a test-owned temporary SQLite database.
- Browser runtime attempts: occupied port 8128 was confirmed with HTTP health (no unrelated server stopped); NO_PROXY did not resolve it, so isolated free ports 18439/15439 were selected. Empty database URL was invalid. SQLite in-memory then exposed thread-local missing tables. Final fallback uses a unique temp file with existing SQLAlchemy metadata created after importing the app; no migration file, migration command, schema change or external database is involved.
- Initial browser error-state test failed because React development StrictMode makes more than one request. Corrected test fault injection to stay failed until Retry is clicked; no application retry behavior or assertion weakened.
- Initial new unit failures exposed currency compact formatting adding .00; corrected formatter to match $660K. Fixed an intersection type for factor contributions and an unused initial assignment flagged by lint; no suppressions.
- Browser visual pass caught page width excluding its negative margins; corrected screen-scoped desktop/mobile widths. Drawer overlays and body scrolling override shared drawer defaults locally, without editing shared components.
- Figma design-to-code skill supplied all seven referenced frames and the exact search/chevron SVG assets. Browser verification skill checklist is followed with user-requested Playwright because agent-browser CLI is not installed; no dependency installed merely to duplicate browser tooling.
- Existing profile/opportunity browser assertions were updated to the accessible dialog, row/card and filter controls while retaining business-context, reload, account navigation, graph and overflow assertions. No old tests removed or weakened. The old absolute screenshot paths remain unchanged because durable startup, not screenshots, is the blocker.
- Drawer close after direct URL reload additionally focuses the selected row if shared Drawer has no initiating element to restore. It does not steal focus from a newly opened dialog. Factor reasons remain accessible text/tooltips without stretching the six-row design.
- Browser evidence verification expects the two development StrictMode effect requests exactly, with zero requests before expansion. This corrects the new test's one-request assumption; evidence fetching code was not changed.
- Final full backend run: 803 passed / 16 failed / 10 errors / 2 skipped. Fifteen failure names and all ten errors match baseline. The baseline durable-monitor test passed, while the unchanged operational-lock timing test failed (it waits only 10ms for a background heartbeat). Targeted rerun of both tests passed (2/2). No scoring/monitor/test changes were made to address this environmental scheduling variation.
- Local font license notices are retained in a CSS legal comment as well as original adjacent OFL files so production font bundles retain their license. Fonts use feature-specific aliases and do not restyle the shared shell.

Further implementation decisions, fallback attempts, and verification results are appended below.
