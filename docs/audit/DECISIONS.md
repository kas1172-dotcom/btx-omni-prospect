# Today priority decisions, 2026-09-20

| Decision | Alternatives considered | Reason |
| --- | --- | --- |
| Apply the provided Phase 3B rules to Today in a single `modules/priority_ordering.py` owner. | Modify unrelated durable Actions scoring or retain internal-first ordering. | Today is the requested surface. The existing Actions module remains a separate consumer workflow; no weighted action score is introduced. |
| Add only optional priority metadata and a default-false `CommercialAlert.hard_stop`. | Replace the response contract or parse IDs in the frontend. | Preserves existing fields and consumers; source-only facts remain source-owned. |
| Missing frontend metadata means undetermined importance, never recomputed High. | Reuse severity or `priority_eligible` as a fallback. | Explicit approval requires server-owned importance. Older payloads still render. |
| `high_importance=false` uses a neutral medium badge. | Infer low/medium from source scores or expose another unapproved importance tier. | The new boolean authorizes High versus non-High only. No frontend score inference. |
| Internal known, CONFIRMED records with evidence have High confidence. Unconfirmed or absent provenance has unknown confidence unless explicit confidence exists. | Treat all records as High regardless of provenance. | Honors the governed-record default without converting uncertain records into confirmed evidence. |
| Ambiguous event kinds are UNKNOWN; structured `risk_severity` establishes RISK even for an otherwise positive event type. | Infer risk from headline prose or assume every public event is an opportunity. | Uses deterministic evidence fields and prevents unknown nature from escalating. |
| Follow-ups and coordination are opportunity-oriented executable work; external-intelligence commercial context is UNKNOWN. | Treat every internal alert as a risk. | Alert existence does not establish a confirmed negative commercial condition. |
| Numeric scores outrank ordinal tier fallback within a class only through the same 0-100 ordering scale; CRITICAL/HIGH/MEDIUM-or-MODERATE/LOW fallbacks are 100/75/50/25. `underlying_score` stays null for tier-only items. | Fabricate exact measured scores from tiers. | Allows deterministic comparisons without presenting ordinal stand-ins as measured values. |
| Incomplete assessments use their explicit upper bound for sorting and report `assessment_complete=false`. Unknown scores sort last. Range ceilings never establish top-band importance. | Default unknown scores to 100 or use a midpoint. | Unknown never inflates; a possible ceiling is not a confirmed severity. |
| A confirmed Class 3 high-severity risk with unknown confidence is High importance but stays Class 3. | Promote it into Class 2 or hide its known severity. | Implements both explicit user rules: unknown confidence cannot escalate class, while Class 3 High risk is high importance. |
| Date ties use due date, then created_at if supplied, else observed_at; absent/invalid dates last. | Invent a due date from a publication or program date. | A public event date is not necessarily an action deadline. Current sources lack due dates. |
| Closed duplicate identities suppress open copies. Ordinary duplicates retain the first deterministic ordered identity; different assessment context IDs remain separate. | Merge by headline/account or allow stale open copies to reappear. | Avoids semantic guessing and resurrection of explicitly closed work. |
| Preserve validation-lane admission, ordering and placement. | Triage validation items with executable priorities. | Explicit boundary in the addendum. |
| Use narrow LF attributes on the three verified byte-hashed inputs. | Change hash constants or normalize the whole repository. | Git blob hashes already match the reviewed constants; content enrichment is unnecessary. |
| The optional Today public demo fixture uses the existing sample clock and SAMPLE label, and is enabled only in development. | Continue using real-time timestamps and LIVE_PUBLIC labels in a fabricated scenario. | Applies the addendum's deterministic sample requirement without changing Monitor or the fixed clock. |
| Carry optional priority metadata into the expanded SignalBriefCard on action rows. | Let the nested card recompute High from priority_eligible. | Prevents contradictory importance inside the same priority; unrelated Intelligence consumers retain their existing contract. |
| Preserve the Today page-tab filter in navigation serialization, and reveal a filtered-out lead card's canonical row on Review. | Keep selection only in memory or let Review target a missing DOM node. | Browser tests exposed these integration gaps in the previous Task A implementation. |
| Use dedicated local PostgreSQL databases in a task-owned container for full and browser suites. | Reuse another agent's database or retain missing-database errors. | Keeps tests isolated and exercises database paths previously unavailable in the baseline. |
| Follow the supplied rubric class rules. | Implement deck slide 15's urgency 30 / BTX impact 30 / dependency 20 / age 10 / ownership gap 10 weighted formula. | The user explicitly rejected those weights. Deck discrepancy is recorded for the owner, not resolved by inventing a blend. |

Scope: the conversation provides Task A plus Phase 1 hash normalization and Phase 3B ordering. No additional Phase 3/4/7 enrichment specification or executive deck file was supplied. No unspecified enrichment was performed.
