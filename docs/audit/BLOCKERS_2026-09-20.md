# Remaining baseline blockers, 2026-09-20

No new failing test IDs versus `baseline_failures_2026-09-20.txt`. Two pre-existing backend acceptance failures remain. No tests were weakened, skipped, or deleted to obtain this result.

| Test | Evidence and remaining issue | Approaches tried |
| --- | --- | --- |
| `tests/test_api_acceptance.py::test_canonical_poc_api_end_to_end_paths` | Assertion at line 140 expects literal `CROSS_BU_COORDINATION` in an Omni answer. `backend/src/btx_omni/modules/assistant/orchestration.py:28` maps that kind to `Business-unit coordination`; the account briefing uses the human-facing label. This is outside Today ordering. | Reproduced baseline; repaired verified checkout line endings without re-pinning; reran full suite against migrated isolated PostgreSQL; inspected assertion and assistant projection. Same failure remains. |
| `tests/test_api_acceptance.py::test_omni_cross_account_score_ranking_uses_typed_market_filter` | Assertion at line 552 expects Account Attractiveness ranking language. `backend/src/btx_omni/modules/assistant/orchestration.py:2778` onward handles scoped opportunity scores, with an explicit missing-complete-opportunity-score response. Test and existing assistant behavior disagree. | Reproduced baseline; restored reviewed input Git bytes; loaded dedicated database; reran full suite; inspected ranking branch. Same failure remains. |

These require separate owner alignment of assistant acceptance semantics, not changes to Today or weakened assertions. Final full run: 814 passed, 2 failed, 0 errors. Baseline: 710 passed, 67 failed, 10 errors. Of 77 baseline failure/error IDs, 75 no longer fail and 2 remain.

No unimplemented in-scope Today behavior is being blocked on a permission request. The separately numbered enrichment phases referred to in the addendum were not supplied as task specifications; no enrichment was invented. The executive deck was not supplied for independent inspection.
